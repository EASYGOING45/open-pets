{
  "name": "openpets",
  "private": true,
  "version": "0.1.0",
  "description": "OpenPets desktop renderer for Codex-format pet atlases.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  }
}
