{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default capabilities for the OpenPets main window",
  "windows": ["main", "picker"],
  "permissions": [
    "core:default"
  ]
}
